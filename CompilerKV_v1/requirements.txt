# CompilerKV_v1
#
# Notes:
# - If you need CUDA, install the matching PyTorch build from https://pytorch.org first.
# - FlashAttention2 is optional (and typically not supported on Windows).

# Core
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.26.0
numpy>=1.23.0
tqdm>=4.66.0
einops>=0.7.0
safetensors>=0.4.0
sentencepiece>=0.1.99

# LongBench metrics / evaluation
jieba>=0.42.1
rouge>=1.0.1
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.25.0

# Utils
jsonlines>=4.0.0

# Plotting (tables/)
matplotlib>=3.7.0

# Optional (FlashAttention2; usually Linux + CUDA only)
# flash-attn>=2.5.0
